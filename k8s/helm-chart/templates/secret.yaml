apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets.name }}
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
{{- range $key, $value := .Values.secrets }}
  {{- if ne $key "name" }}
  {{ $key }}: "{{ $value }}"
  {{- end }}
{{- end }}
