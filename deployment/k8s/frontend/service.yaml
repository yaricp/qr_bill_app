apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: qracun
    tier: frontend
spec:
  selector:
    app: qracun
    tier: frontend
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
